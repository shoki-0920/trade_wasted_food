<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Trade Wasted Food</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <!-- Tailwind CSS を読み込む -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

  <%= javascript_importmap_tags %> 
</head>
<body class="bg-gray-100">
<header class="bg-blue-500 text-white p-2 flex justify-between items-center">
  <%= button_to 'Login', '/auth/auth0', method: :post, authenticity_token: true %>
  <%= button_to 'Logout', 'auth/logout', method: :get %>

  <%= link_to root_path do %>
  <%= image_tag "logo.svg", alt: "ロゴ", class: "h-12" %>
  <% end %>
  </header>

  <main class="container mx-auto p-4">
    <%= yield %> <!-- ここに個別のビューがレンダリングされます -->
  </main>

  <footer class="bg-gray-800 text-white text-center p-4 mt-4">
    <p>&copy; 2025 Trade Wasted Food</p>
  </footer>
</body>
</html>
